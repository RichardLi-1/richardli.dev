import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "SaFuture & Qwhery",
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
